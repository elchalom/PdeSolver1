#include <stdint.h>
#include <math.h>

int* main(float rx, float vx, float ry, float vy, float dt)
{
    int i = 0;

    while (ry > 0)
    {
        // write this code after I figure out how to do thrust properly
    }
}